# Azure OpenAI Configuration
# Get your Azure OpenAI endpoint from Azure Portal
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-4o
AZURE_OPENAI_API_VERSION=2025-01-01-preview

# Azure AI Foundry Project Configuration
# Get this from Azure AI Foundry portal or using: az ml workspace show --name <project-name> --resource-group rg-legobot
AZURE_AI_PROJECT_ENDPOINT=
PROJECT_CONNECTION_STRING=

# Azure Storage Configuration (optional, for blob storage)
SUSTINEO_STORAGE=

# Authentication
# This project uses Azure CLI credentials by default
# Before running tests, authenticate with: az login
# Make sure you have access to the rg-legobot resource group
